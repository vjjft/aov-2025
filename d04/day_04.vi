
use #util::Channel;
use #util::io::{log, out};
use #root::ops::Cast;
use #root::data::Map;

pub fn main(&io: &IO) {
  let part1 = 0[N64];
  let part2 = 0[N64];

  let floor = Floor::parse_input(&io);

  // io.log(floor);
  io.out(&floor as String);

  io.out(floor.show() as String)
  floor.notify()
  io.out(floor.count_can_lift() as String)

  io.println("Part 1: {part1}");
  io.println("Part 2: {part2}");
}

enum* Dir {
  N_(),
  S_(),
  E_(),
  W_(),
  NW(),
  NE(),
  SW(),
  SE(),
}
mod Dir {
  use #root::ops::comparison::Ord::{Ord, Eq, Gt, Lt};
  // pub impl show: Show[Dir];
  pub impl ord: Ord[Dir] {
    fn cmp(&a: &Dir, &b: &Dir) -> Ord {
      match (a) {
        N_() {
          match (b) {
            N_() { Eq() }
            S_() { Eq() }
            E_() { Eq() }
            W_() { Eq() }
            NW() { Eq() }
            NE() { Eq() }
            SW() { Eq() }
            SE() { Eq() }
          }
        }
        S_() {
          match (b) {
            N_() { Eq() }
            S_() { Eq() }
            E_() { Eq() }
            W_() { Eq() }
            NW() { Eq() }
            NE() { Eq() }
            SW() { Eq() }
            SE() { Eq() }
          }
        }
        E_() {
          match (b) {
            N_() { Eq() }
            S_() { Eq() }
            E_() { Eq() }
            W_() { Eq() }
            NW() { Eq() }
            NE() { Eq() }
            SW() { Eq() }
            SE() { Eq() }
          }
        }
        W_() {
          match (b) {
            N_() { Eq() }
            S_() { Eq() }
            E_() { Eq() }
            W_() { Eq() }
            NW() { Eq() }
            NE() { Eq() }
            SW() { Eq() }
            SE() { Eq() }
          }
        }
        NW() {
          match (b) {
            N_() { Eq() }
            S_() { Eq() }
            E_() { Eq() }
            W_() { Eq() }
            NW() { Eq() }
            NE() { Eq() }
            SW() { Eq() }
            SE() { Eq() }
          }
        }
        NE() {
          match (b) {
            N_() { Eq() }
            S_() { Eq() }
            E_() { Eq() }
            W_() { Eq() }
            NW() { Eq() }
            NE() { Eq() }
            SW() { Eq() }
            SE() { Eq() }
          }
        }
        SW() {
          match (b) {
            N_() { Eq() }
            S_() { Eq() }
            E_() { Eq() }
            W_() { Eq() }
            NW() { Eq() }
            NE() { Eq() }
            SW() { Eq() }
            SE() { Eq() }
          }
        }
        SE() {
          match (b) {
            N_() { Eq() }
            S_() { Eq() }
            E_() { Eq() }
            W_() { Eq() }
            NW() { Eq() }
            NE() { Eq() }
            SW() { Eq() }
            SE() { Eq() }
          }
        }
      }
    }
    fn lt(&a: &Dir, &b: &Dir) -> Bool {
      a < b
    }
    fn le(&a: &Dir, &b: &Dir) -> Bool {
      a <= b
    }
  }
}

type OptChan[M] = Option[&Channel[M]];
type Neighborhood[M] = Map[Dir, OptChan[M]];
struct* RollState(Bool);
mod RollState {
  pub impl show: Show[RollState];
}
type NH = Neighborhood[RollState];
mod NH {
  pub fn new() -> NH {
    Map::empty[Dir, OCRS]
  }
  pub impl show: Show[NH] {
    fn show(_: &NH) -> Show {
      Show::Literal("")
    }
  }
}

struct? Roll({ state: RollState, nh: NH });
mod Roll {
  pub fn parse_char(c: Char) -> Option[Roll] {
    let state = RollState(when {
      c == '@' { true }
      c == '.' { false }
      _ {
        return None();
      }
    });
    Some(Roll({ state, nh: NH::new() }))
  }
  pub fn parse_char_assume(c: Char) -> Roll {
    parse_char(c).assume()
  }
  pub impl to_char: Cast[&Roll, Char] {
    fn cast(&roll: &Roll) -> Char {
      if roll.state! == true {
        '@'
      } else {
        '.'
      }
    }
  }
  pub fn check_dir(x: OCRS) -> Bool {
    match (x) {
      None() { false }
      Some(_) { true }
    }
  }

  pub fn .count_neighbor_rolls(&roll: &Roll) -> N32 {
    let fn* check(total: N32, (_, nb): (Dir, OCRS)) -> N32 {
      total + (check_dir(nb) as N32)
    }
    // let x = roll.nh;
    // let ir = x.iter_ref();
    // ir.fold(0, check)
    let total = 0;
    for (_, nb) in roll.nh.iter() {
      total += (check_dir(nb) as N32)
    }
    total
  }

  pub fn send(o: OCRS, s: RollState) {
    if o is Some(c) {
      c.*.send(s);
    }
  }

  pub fn .notify(&roll: &Roll) {
    // let fn* notify(dir) {
    //   Roll::send(dir, roll.state)
    // }
    // roll.nh.iter_ref().for_each(notify);
    for (_, nb) in roll.nh.iter() {
      Roll::send(nb, roll.state)
    }
  }

  // pub impl show: Show[Roll];
  pub impl show: Show[Roll] {
    fn show(&r: &Roll) -> Show {
      Show::Literal("here is a roll being shown")
    }
  }
}

type OCRS = OptChan[RollState];

struct? Floor(pub List[&List[&Roll]]);
mod Floor {
  pub fn parse_input(&io: &IO) -> Floor {
    let floor = Floor([]);

    let row_above: List[&Roll] = [];
    let roll_nw: Option[&Roll] = None();
    let roll_w: Option[&Roll] = None();

    let fn* init_roll(c: Char) {
      let roll = Roll::parse_char_assume(c);

      let roll_n = row_above.pop_front();
      let roll_ne = row_above.get(0);
      if roll_nw is Some(&roll_nw) {
        let (a, b) = init_channel_pair();
        roll.nh.insert(Dir::NW(), a);
        roll_nw.nh.insert(Dir::SE(), b);
      }
      // if roll_n is Some(&roll_n) {
      //   (roll_n.nh.s, roll.nh.n) = init_channel_pair()
      // }
      // if roll_ne is Some(&roll_ne) {
      //   (roll_ne.nh.sw, roll.nh.ne) = init_channel_pair()
      // }
      // if roll_w is Some(&roll_w) {
      //   (roll_w.nh.s, roll.nh.w) = init_channel_pair()
      // }

      roll_nw = roll_n;
      roll_w = Some(&roll);
      &roll
    }
    while io.read_line() is Some(line) {
      let row = [];
      while line!.pop_front() is Some(c) {
        row ++= [init_roll(c)]
      }
      row_above = row
      floor!.push_back(&row)
    }
    floor
  }

  pub fn .notify(&floor: &Floor) {
    floor.apply(Roll::notify)
  }

  pub fn .apply[F*; fn F(&Roll) -> ()](&floor: &Floor, f: F) {
    for &&row in floor!.iter_ref() {
      for &roll in row.iter_ref() {
        f(roll)
      }
    }
  }

  pub fn .count_can_lift(&floor: &Floor) -> N32 {
    let count = 0;
    for &&row in floor!.iter_ref() {
      for &roll in row.iter_ref() {
        if roll.*.state! {
          continue;
        }
        if (4 > Roll::count_neighbor_rolls(roll)) {
          count += 1
        }
      }
    }
    count
  }

  fn init_channel_pair() -> (OCRS, OCRS) {
    let (a, b) = Channel::new_pair[RollState]();
    (Some(a), Some(b))
  }

  pub impl to_string: Cast[&Floor, String] {
    fn cast(&floor: &Floor) -> String {
      let str = "";
      for &&row in floor!.iter_ref() {
        for roll in row.iter() {
          str!.push_back(roll as Char);
        }
        str!.push_back('\n');
      }
      str
    }
  }

  pub impl show: Show[Floor];
}
